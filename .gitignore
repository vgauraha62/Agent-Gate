# Zig build cache (per-project)
.zig-cache/

# Build output (binaries and artifacts)
zig-out/

# Dependencies (downloaded by zig)
dependencies/

# Editor and IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# GPG keys (never commit)
my-gpg-key1.asc

# Analysis and tracking files
analysis_minimax.md
commands.txt
commands_analysis.txt
progress.md
task_plan.md
findings.md
test_script.txt
*_summary.txt
STORAGE_STATUS.txt

# Day tracking folders
day5/
home/
project/
PENDING_REPORTS/

# Graphify output
graphify-out/

# Zig global cache (cross-project)
# ~/.cache/zig/

# Local config with secrets (never commit)
config.json

# License server RSA private key (never commit)
keys/

# Environment secrets (never commit)
.env

